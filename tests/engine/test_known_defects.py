"""Reproductions of defects that are understood but not yet fixed.

Each test here fails on purpose. Remove the xfail marker with the fix, never the test.

This file is empty at present. The clarification-then-image deadlock it held is fixed;
its test now lives in test_component_integration.py as a passing acceptance test.
"""
