# Voice/perception checkpoint — 24 September 2026

Branch: `atishay/perception`, after merging local `main` (`749fe23`) at `f84bea7`.
This is Atishay-owned component and demo evidence, not a Samsung submission run.

## Reproductions and changes

- Before the owned policy fix, four focused assertions failed: finalized “Stop speaking while I think” and “Stop the washing machine” both became task `stop`; partial “Cancel this booking” and “Stop the whole task now” also became task `stop`.
- The owned policy now reserves `stop` for a narrow, **final** task-cancel phrase. A final device command such as “Stop the washing machine” is a normal complete request. “Stop speaking” is held as `continue` with maximum uncertainty, because the current shared `TurnDecision` has no output-only scope. This prevents accidental task cancellation but **does not implement spoken output stop**. Mridul must add/approve that typed effect under C24-2.
- The configured demo reasoner test now gives image-only and image-plus-question paths different answers, waits for the question's observed `event_id`, and requires the final's `caused_by_event_id` to match it. The existing browser projection test separately rejects old, unattributed and duplicate finals. This is deterministic loopback/mock evidence, not Qwen or a confirmed tool effect.

## Actual local ASR; generated audio only

Model: installed `Systran/faster-whisper-base.en` snapshot `3d3d5dee26484f91867d81cb899cfcf72b96be6c`, Faster Whisper 1.2.1, CPU INT8 on Intel Core Ultra 5 125H, Python 3.12.10. The source is the pre-existing generated `tests/fixtures/audio/synthetic_pause_correction.wav` (SHA-256 `49B0B26FD1EBCAE0772B2559A4ABA3782444F59FAB7E7E7038122F06157C872B`). It is **not** a human microphone recording or a new held-out case.

Direct `WhisperModel.transcribe(..., beam_size=5, word_timestamps=True)` took 2.215 s after model construction. It emitted “Book Tuesday.” at 0.00–0.62 s and “Actually, Wednesday at 5.” at 2.64–5.04 s. Its word probabilities for Tuesday, Wednesday and `5` were approximately 0.945, 0.999 and 0.846. Those are backend decoder estimates, **not calibrated correctness confidence**. The same model returned zero segments for the 0.5-second generated tone in 1.495 s. These timings are single runs, not latency distributions.

The real `LocalPerception.observe(AudioEvent)` path on the correction clip took 3.356 s including adapter/model initialization and produced `faster-whisper/cpu-int8`, text `Book Tuesday. Actually, Wednesday at 5.`, `final=True`, source `correction-1`, revision 0 and **speech_start/speech_end both 0.0**. This verifies the present seam flattens useful segment timing/uncertainty and treats the complete WAV as final. It does not prove an official two-clip turn, semantic slot resolution or a booking.

The local browser served the demo with this ASR model at `http://127.0.0.1:8000/` and connected to its WebSocket. Start voice did not enter capture: Chromium reported microphone permission `prompt`, and the permission UI was not exposed to this test control. **No physical audio was captured or transcribed in this checkpoint.** A manual permission/speech pass remains required. `ollama` was not found on PATH, no configured vision/reasoner model environment variable was present, and nothing listened on port 11434; no live vision or Qwen result is claimed.

## C24 decision packet — both Atishay and Mridul must agree

| ID | Decision and example to agree | File split and independent next work |
|---|---|---|
| C24-1 official audio bridge | Two MP3 clips share utterance `u1`: clip 0 has `end_of_turn=false`; clip 1 has `end_of_turn=true` and corrects Tuesday to Wednesday. Define rooted admission, decoder owner, aggregate vs clip timestamps, byte/duration cap, finality/revision, interruption and cleanup. Do not finalize clip 0 or use organizer `_reference_text`. | Mridul: Samsung protocol/adapter, decoder/assembly, shared fields and boundary tests. Atishay: ASR/worker observations, revision tests and real-media measurements. Decoder ownership remains **unagreed**. |
| C24-2 speech clock and stop scope | Capture/activity timestamps need a declared clock conversion, including unknown endpoints. Final “Stop speaking” stops output only; final “Cancel this booking” cancels the task; “Stop the washing machine” is a device request; partial cancel text cannot stop the task. | Mridul: additive typed decision/interrupt contract, controller effects and timing metric. Atishay: acoustic facts, policy classification, browser capture and owned tests. Current policy's output-only `continue` is a safety interim, not completion. |
| C24-3 common runtime and causal replies | A newer spoken request `u2` arrives after an old image-only plan. The old final may be logged but must not render/speak for `u2`; a confirmed mock effect needs matching input, action and result IDs. Agree one configured factory/profile and dynamic tool manifest. | Mridul: reusable factory/configuration, manifest and authoritative IDs/effects. Atishay: demo adapter, browser projection and gated integration tests. The current empty-manifest mock demo is not the Samsung/Qwen runtime. |
| C24-4 image meaning and authority | Frame `f1` precedes the question, then `f2` replaces it; a contradiction with a user-confirmed slot requires clarification, not a write. Decide whether official frames wait for the next question while standalone demo frames may answer after debounce. | Mridul: readiness, conflict state, write guard and engine tests, including his four failing pending-frame probes. Atishay: frame/provenance observations, display and owned integration tests. The existing conflict xfail remains visible. |
| C24-5 media-to-task benchmark | For each consented recording/image, map media ID, SHA-256, format and label/exposure timestamp to a fresh task and effect oracle. A 60-case routing catalog is not 60 independently completed tasks. | Atishay: media provenance, ASR/activity/vision errors and exposure ledger. Mridul: executable task/effect oracles, official repeated runs, package and aggregation. No human set or new official run was produced here. |

## Checks and missing inputs

- `pytest tests/perception tests/demo -q`: 371 passed, 1 retained xfailed, 2 dependency warnings.
- Full `pytest -q`: 1203 passed, 2 skipped, 1 retained xfailed, 2 dependency warnings in 96.62 s.
- Focused reasoner correlation: 1 passed; `ruff` on changed owned paths, Node speech-lifecycle check and `git diff --check`: passed.
- The kit files named in the review (`WALKTHROUGH.md`, `docs/PROTOCOL.md`, `docs/SCORING.md`, `docs/SUBMISSION.md`) were absent at the documented `../participant-kit/participant-kit/` location and nearby Downloads searches. The merged readiness review was read, but this does **not** substitute for reading the kit. Obtain its location before claiming protocol-complete acceptance.
- Next: physical-mic permission and consented speech observation; an additive C24-1/2 contract decision; real vision backend availability; then common-runtime/effect integration and raw public media with Mridul.

## Capture permission-wait follow-up

The live local demo was refreshed after a browser-side capture fix. Before the fix, two `startMicrophone()` calls while `getUserMedia()` was unresolved made **two** permission requests and left the visible control saying “Start voice.” The owned Node regression failed at `2 !== 1`. After the fix, the browser visibly says **“Waiting for microphone permission”** and **“Recording has not started yet”** while the single request is pending; Start is temporarily disabled, and typed input remains available. A rejected permission request restores the control with one recoverable error. If a pending request is invalidated by a new task, disconnect or page exit, a later granted stream is stopped before recording begins; the deterministic test verifies one stopped track and no false error.

This is browser behavior and a simulated permission promise, **not** proof that the physical microphone worked. The in-app browser still did not surface an actionable permission dialog, so no raw human voice or audible playback was measured. No ASR, controller, Samsung protocol or shared-contract code changed in this follow-up.

An additional owned disconnect reproducer found that an already-started recording was not torn down when the WebSocket closed. The new `discardMicrophone()` path closes the recorder port, disconnects audio nodes, stops the physical tracks, closes the AudioContext and clears the unsent WAV. Session restart and page exit use the same teardown. A deterministic Node regression checks teardown calls, idempotency and disconnect/exit wiring. This is a simulated stream test; physical disconnect while recording is still unverified.

The same regression then reproduced a close race: if the WebSocket closed while `stopMicrophone()` awaited AudioContext shutdown, the old code encoded and staged a WAV after disconnect. A closed/closing socket now prevents that staging. The before-fix assertion observed one unwanted encode; it passes after the guard. This is still a simulated race, not a measured network or device run.

Final checks for this browser capture slice: `tests/demo tests/perception` **373 passed, 1 retained xfailed**; full repository **1205 passed, 2 skipped, 1 retained xfailed**, with two dependency deprecation warnings in 81.24 s. Repository Ruff, inline JavaScript parse and whitespace check passed.

## Read-only teammate branch update and proposed vision experiment

After the capture commit, `origin/mridul/engine` advanced to `81699c9`; this is **not merged into local main or this branch**. Its `SAMSUNG_AUDIO_ADMISSION_2026-09-24.md` reports an A-owned MP3/end-of-turn bridge and controller-only pending/failed speech status. B still receives completed WAV observations; A explicitly says browser timing/uncertainty and teammate approval remain open. Its configured-agent factory is available on that branch for later demo adoption, but the existing main demo does not use it. No A source was copied or edited here.

Mridul's retained public-frame observation took **23.688 s** on his declared warm Gemma 3/Ollama setup, and the public visual scenario missed the six-second tail; this is his reported measurement, not a rerun on this checkout. On this machine no service listened on the documented 11435 port, and no local Ollama executable was found in the inspected locations, so B cannot claim a fresh real-vision latency result. A larger timeout alone would not establish timely image understanding.

**Both Atishay and Mridul must agree before changing vision interfaces:** propose an optional `--vision-max-output-tokens` on B's assigned worker, with an explicit positive integer value and no changed default. The A-owned provider/config/package side would map it to Ollama chat `options.num_predict`, validate/freeze the selected setting and preserve the exact backend/model label; B would test the worker boundary and actual pixel-grounded results. The [official chat API](https://docs.ollama.com/api/chat) accepts runtime `options` and reports total, load, prompt-evaluation and generation durations; Ollama's [parameter reference](https://docs.ollama.com/modelfile#valid-parameters-and-values) defines `num_predict` as the maximum generated tokens. This is a **proposal**, not an implemented or measured speedup. Compare uncapped, 64-token and 128-token outputs on the same hashed unfamiliar PNGs with a warm service; retain unreadable/ambiguous cases, text truncation, factual errors, timing breakdown and source/revision IDs. If image preparation or prompt evaluation dominates, a generation cap may not fix the latency. No capped output should silently become write authority, and an incomplete/truncated model response must be reported honestly.

## Direct ASR evidence slice

An opt-in owned probe now preserves the installed Faster Whisper model's segment and word offsets plus decoder estimates in JSON without changing live observations or the agent. The checked-in generated repetition clip preserved both `Tuesday` tokens and the correction to `Wednesday`; the pause/correction clip yielded two timed segments; a generated tone yielded no decoded segments. Exact local-model timing, fixture hashes, backend, probabilities and limits are in `docs/feedback/ASR_MEASUREMENTS.md`. The dedicated injected-model tests cover repetition, zero segments and malformed text. This is actual local model inference on generated WAVs, **not** human microphone audio or Samsung MP3/task evaluation.

**C24-1/2 decision still jointly required:** Atishay can supply ASR evidence and candidate per-word/segment timestamps; Mridul owns the shared observation/audio fields, official MP3 assembly and controller semantics. Both must agree whether offsets refer to a clip or an assembled utterance, how they map to the session clock, how partial/final revisions and unavailable endpoints are encoded, and what uncertainty may affect an action. Decoder probabilities are not calibrated confidence. The probe deliberately never marks a turn final or updates `Observation`.

## Partial-hypothesis timing metric correction

The owned offline endpoint replay previously counted a **partial** transcript's provisional `speech_end_s` as a matched final when running the ungated acoustic baseline. A deterministic case has activity at 0–1.0 s and 1.8–2.4 s, a partial revision available at 1.0 s, and the true final revision only available at 3.2 s—after the recording ends at 3.0 s. Before the fix, its two acoustic candidates were visible as expected, but the report incorrectly assigned an `added_wait_after_speech_end_s` and could report zero missed finals. The new test failed on the non-null wait. Now the candidates and available revision identity remain visible, while final-speech-end wait stays unknown and `missed_final_count=1`. Focused replay: 8 passed; Ruff and whitespace check passed.

This repairs an **offline measurement**, not streaming finality, the controller, or the browser. Real physical microphone capture is still pending: the open local demo reached “Waiting for microphone permission” and “Recording has not started yet”; no permission was granted or human audio captured in this run. The computer-use permission boundary requires the user to act on that prompt. C24-2's clock/finality and output-only interruption semantics still need joint Atishay/Mridul agreement.

## Superseded-final timing follow-up

The same offline replay had a correction failure: a final revision for an earlier utterance part could satisfy `missed_final_count` and provide aggregate wait even after a newer final revision superseded it. A deterministic two-pause example keeps final revision 1 available at 1.0 s and changes the availability of corrected final revision 2 (speech end 2.4 s). Before the fix, both new cases failed: when revision 2 arrived at 2.6 s, aggregate wait incorrectly used revision 1's 0.8 s instead of revision 2's 0.6 s; when revision 2 arrived at 3.2 s, after the 3.0 s recording end, the report incorrectly said zero missed and still showed 0.8 s. The aggregate now targets **the newest revision only if it is final**: 0.6 s and zero missed in the first case; unknown wait and one missed final in the second. Per-candidate history still shows revision 1's earlier endpoint. A newer partial also invalidates aggregate wait from an older final. Focused replay: 10 passed; Ruff and diff check clean.

This is a deterministic revision-timing metric repair, not proof of real ASR revision delivery, endpoint quality, spoken interruption or correct booking. C24-1/2 requires Atishay and Mridul to agree the official clip/utterance clock, revision and finality contract before a similar rule can affect the agent.

## Installed-ASR child worker into the actual Agent controller

On generated WAVs, `ProcessPerception` ran the installed Faster Whisper base.en model in its real child process, returned source/event/backend-tagged observations, reused the child for the second call and terminated it on close. A separate generated-WAV run reached the actual `Agent` controller; its mock reasoner saw the recognized correction and the final was caused by the intended audio event. There were **zero** tool calls/effects. Exact hashes, transcripts and single-run times are in `docs/feedback/ASR_MEASUREMENTS.md`. An opt-in owned regression now preserves this seam when a local model path is provided; it passed on this installed snapshot.

The opt-in regression now uses Atishay's `HeuristicTurnPolicy`; it verifies the corrected generated transcript produces an acknowledgment and a final caused by that same audio event. This narrows the C24-3 integration evidence but **does not satisfy the common real-runtime gate**: reasoning remained a deterministic mock, no dynamic tool manifest or confirmed mock write existed, and Mridul's configured factory remains unmerged into this branch. The real worker still marked the completed WAV observation final with zero speech endpoints; C24-1/2 clip assembly, clock and finality require joint agreement. No physical microphone, live vision or official raw-media task result was produced.

## 2026-09-25 — Booking cancel versus task stop

**Reproduction:** The existing `test_stop_words_have_distinct_scopes` expected “Cancel this booking” to return `stop`. In the actual engine, that decision cancels the entire agent task, so an ordinary request to cancel a booking could never reach the planner. The focused regression failed before the fix (`stop` observed; `complete` required).

**Owned fix:** `HeuristicTurnPolicy` now reserves `stop` for explicit task-level language such as “Cancel this task” or “Stop the whole task”; “Cancel this booking” and “Cancel the booking” are ordinary completed user requests, as is “Stop the washing machine”. “Stop speaking” remains non-task-canceling because the current decision contract lacks the agreed output-only stop scope. No shared contract or engine behavior changed.

**Verification:** Focused policy suite 22 passed; full suite 1215 passed, 3 skipped, 1 xfailed, 2 existing dependency deprecation warnings in 64.36 s; Ruff and diff checks clean. One skip is the opt-in local-ASR worker test because this run did not have its model path configured. This repairs lexical scope classification only; it does not establish booking behavior or real interruption. A user-facing local microphone run sheet is in `docs/feedback/MICROPHONE_TEST_PROTOCOL.md`; no human audio was recorded in this checkpoint.

## No-speech continuation — 2026-09-25

The user asked not to speak. No human audio was captured or uploaded. The open local page remained at browser permission `prompt`; reloading canceled the pending request and left the microphone off. No Ollama process/model manifest or local vision service was available, so no image-inference claim is made. The generated-audio opt-in test now exercises the installed ASR worker through the real Agent loop and Atishay-owned `HeuristicTurnPolicy`: the corrected transcript produces an acknowledgment and final correlated to the audio event, with no tool effects. Full suite with the local model path set: 1212 passed, 2 skipped, 1 xfailed in 71.87 s; two pre-existing dependency deprecation warnings. This still leaves human mic, live vision, official raw media, and the common configured runtime unverified.
